pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               
                dir('Extent_report') {
                    bat 'mvn clean compile'
                }
            }
        }

        stage('Test') {
            steps {
                dir('Extent_report') {
                    bat 'mvn test'
                }
            }
        }

        stage('Report') {
            steps {
                dir('Extent_report') {
                    // This publishes the HTML report generated by Maven/TestNG
                    publishHTML([
                        allowMissing: false,
                        alwaysLinkToLastBuild: true,
                        keepAll: true,
                        reportDir: 'target',
                        reportFiles: 'index.html',
                        reportName: 'Extent Report'
                    ])
                }
            }
        }
    }
}
